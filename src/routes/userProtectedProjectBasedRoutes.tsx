/**
 * USER_PROTECTED_PROJECT_BASED_ROUTES: consists of all user protected project based pages inside user platform
 *
 * HERE YOU ADD YOUR PROJECT BASED PAGES
 */

// LIBRARIES
// import { Route } from "react-router-dom";

// import Events from "@/pages/users/Events";

export const UserProtectedProjectBasedRoutes = () => {
  return (
    <>
      {/* <Route path=":id/event/:eventId" element={<Events />} /> */}
    </>
  );
};
